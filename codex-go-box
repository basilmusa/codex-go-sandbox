docker run -it \
    --rm -v "$PWD:/workspace" \
    --user "$(id -u):$(id -g)" \
    -e HOME=/home/coder \
    -v "$HOME/.codex:/home/coder/.codex" \
    -v "$PWD:$PWD" \
    -w "$PWD" \
    codex-dev-agent
